% Vendoring capstone: compiles the
% real upstream `annot` package (dist-v01/packages/annot.satyh) through the
% full loader. Not itself a document — proves `Annot`'s sealed sig (with its
% one optional-arg-rows command row, `\href`) elaborates,
% type-checks, and evaluates through the real pipeline.
@require: annot
1
