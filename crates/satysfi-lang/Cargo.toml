[package]
name = "satysfi-lang"
description = "Abstract syntax tree, elaboration, evaluator, and primitives for SATySFi"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
satysfi-syntax.workspace = true
satysfi-backend.workspace = true
thiserror.workspace = true
# Raster image decoding for `load-image` (docs/plans/math-images.md §Slice 1).
image.workspace = true
# Knuth-Liang hyphenation (D4, docs/plans/text-rendering.md §3): en-US
# patterns only, embedded at compile time (no dictionary files to ship at
# runtime) — the same Liang/TeX pattern data upstream's
# `dist/hyph/english.satysfi-hyph` carries.
hyphenation = { version = "0.8.4", features = ["embed_en-us"] }
# 0.1.0 Slice 1 finale (v1-finale-spec.md §5.1): `compile_document_v1`'s
# signature is fixed to `files: &[satysfi_loader::LoadedFile]` (un-merged,
# plural), so this is now a real dependency, not just a test harness one.
# No cycle: satysfi-loader depends only on satysfi-syntax.
satysfi-loader.workspace = true
