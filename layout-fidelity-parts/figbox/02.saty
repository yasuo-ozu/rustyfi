@require: stdjareport
@require: itemize
@require: annot
@require: code
@require: gr

@require: easytable/easytable
@require: enumitem/enumitem

@require: base/typeset/base
@require: figbox/figbox

open FigBox
open EasyTableAlias

let-inline \link url text =
  let ibf ctx =
    let ctx = ctx |> set-text-color (Color.of-css `darkblue`) in
    read-inline ctx text
  in
  {\href(url){\eval(ibf);}}

let-block ctx +ctx ctxflst bt = read-block (ctxflst |> List.fold-left (fun ctx ctxf -> ctxf ctx) ctx) bt

let document record content = StdJaReport.document record '<%
  +ctx[
    set-font-size 12pt;
    set-paragraph-margin 12pt 12pt;
    set-leading (12pt *' 1.6);
    set-code-text-command (command \code);
  ](content);
>%

let-block ctx +block-frame bt =
  let decoset = VDecoSet.simple-frame 0.5pt Color.black Color.white in
  block-frame-breakable ctx (5pt, 5pt, 10pt, 10pt) decoset (fun ctx -> read-block ctx bt)

let-block +code code = 
  '<+ctx[set-font-size 10pt; set-paragraph-margin 6pt 6pt; set-leading 16pt;]<+Code.code(code);>>

let-block +example str bt =
  '<%
    +code(str);
    +block-frame(bt);
  >%

let-inline \pang = {The quick brown fox jumps over the lazy dog.}

let-inline \lipsum = {
  Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
  Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.
  Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur.
  Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.
}

let-inline \jugem = {
寿限無，寿限無，五劫のすりきれ，
海砂利水魚の水行末・雲来末・風来末，
食う寝るところに住むところ，
やぶら小路のぶら小路，
パイポパイポ，パイポのシューリンガン，
シューリンガンのグーリンダイ，
グーリンダイのポンポコピーのポンポコナーの長久命の長助．
}


in
document (|
  title = {Part 02};
  author = {test};
|) '<
+chapter{がぞう さいず の りふぼっくす を あつかう}<%

  +p{
    「ずぎゅう」 として おもい うかぶ だいひょうてき な もの は がぞう です。
    ほん ぱっけーじ において、 がぞう は `include-image` および `include-image-with-height` をもちいて よみ こむ こと ができます が、
    ここ では がぞう ふぁいる を ともなわず に おおきさ だけ を していできる `dummy-box` をもちいて せつめい します。
    ずぎゅう の おおきさ が はんめい して さえ いれば、
    まだ ずぎゅう そのもの が なく ても ずぎゅう を じょ いた れいあうと が さいげん でき て べんり です。
  }

  +example(```
  +fig-center(dummy-box 200pt 100pt);
  ```)<%
  +fig-center(dummy-box 200pt 100pt);
  >%

  +p{
    ほんらい、 そうにゅう する がぞう は たて または よこ の おおきさ を していする こと により がぞう の さいず が きまる もの です。
    `dummy-box` も どうよう に よこはば と たてはば を ちょくせつ していする こと で、
    さまざま な さいず の ずぎゅう を しめん に はいち する こと ができます。
  }

  +example(```
  +fig-center(dummy-box 100pt 100pt);
  +fig-center(dummy-box 150pt 60pt);
  ```)<%
  +fig-center(dummy-box 100pt 100pt);
  +fig-center(dummy-box 150pt 60pt);
  >%

>%
>
