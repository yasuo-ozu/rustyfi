/target
*.pdf
# ...except the committed layout-fidelity comparison renders: for each corpus
# doc, the port's output (<doc>.port.pdf) beside the original SATySFi's
# (<doc>.satysfi.pdf), regenerated by scripts/layout_fidelity.py --out-dir.
!/layout-fidelity-pdfs/*.pdf
/.claude/
/scripts/.fontcache/
# SATySFi cross-reference cache written next to a built .saty
*.satysfi-aux
